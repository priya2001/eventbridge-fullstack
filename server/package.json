{
  "name": "server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node --watch --env-file-if-exists=.env src/index.js",
    "start": "node --env-file-if-exists=.env src/index.js"
  },
  "engines": { "node": ">=22.9.0" },
  "dependencies": {
    "cors": "^2.8.6",
    "express": "^5.1.0",
    "mongoose": "^9.9.5"
  }
}
